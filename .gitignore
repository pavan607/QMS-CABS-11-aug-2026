# Dependencies
/node_modules
# Local npm cache: bundled in repo for air-gapped installs (refresh with npm run vendor:npm-cache)
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/
.next
out

# Production
/build
dist

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files (.env is intentionally tracked for CABS offline deploy)
.env*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode
.idea
*.swp
*.swo
*~

# Logs
logs
*.log

# OS
Thumbs.db

# Deploy runtime marker (authorized stop)
deploy/qms.intentional_stop

# Customer standalone package (built artifacts — no source, large binaries)
deploy/QMS-Standalone/
deploy/QMS-Standalone.zip

# Uploads
/public/uploads/*
!/public/uploads/.gitkeep

# SQL ignored by default; keep migrations and DB Backup for CABS deploy
*.sql
!database/migrations/
!database/migrations/**
!DB Backup/
!DB Backup/**
deploy/qms.tar

# Office lock files and temp CSS
~$*
tmp-*.css
